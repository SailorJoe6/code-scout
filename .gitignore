# Binaries (only in root directory)
/code-scout
cmd/tei-wrapper/tei-wrapper
*.exe
*.dll
*.so
*.dylib

# Test binaries
*.test

# Coverage
*.out

# Go workspace file
go.work

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Code Scout vector databases (per-project, local only)
.code-scout/

# Code Scout project-specific config (may contain sensitive endpoints/credentials)
.code-scout.json
!.code-scout.json.local.example
!.code-scout.json.external.example

# LanceDB native libraries (downloaded via script)
lib/
include/

# Build artifacts
dist/

# Local Go build cache when tests run with custom GOCACHE path
.gocache/
.code-scout.json.dgx_spark.ollama

# Ralph logs
ralph-error.md
ralph-output.md
tei-wrapper.log
